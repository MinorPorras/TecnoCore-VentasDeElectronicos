@model TECO_M_Marca

@{
    ViewBag.Title = "Actualizar Marca";
    Layout = "_LayoutAdmin";
}

<div class="createContainer">
    <h2 class="pageTitle">Actualizar Marca</h2>
    <form asp-action="Edit" method="post" class="modifyElement createMarcaForm">
        @Html.AntiForgeryToken()
        <input type="hidden" name="controller" id="controller" value="Marcas">
        <input type="hidden" name="action" id="action" value="Edit">
        <input type="hidden" id="TN_Id" name="TN_Id" value="@Model.TN_Id">
        <div class="formgroup">
            <label for="TC_Nombre">Nombre:</label>
            <input type="text" name="TC_Nombre" id="TC_Nombre" value="@Model.TC_Nombre" required class="inputBase">
        </div>
        <div class="formgroup">
            <label for="TB_Activo">Estado:</label>
            <select name="TB_Activo" id="TB_Activo" class="inputBase">
                <option value="true" selected="@(Model.TB_Activo ? "selected" : null)">Activo</option>
                <option value="false" selected="@(!Model.TB_Activo ? "selected" : null)">Inactivo</option>
            </select>
        </div>
        <div class="btngroup">
            <a asp-action="Index" class="btnExit">Regresar</a>
            <button type="submit" id="updateBtn" class="btnEdit">Actualizar</button>
        </div>
    </form>
</div>
