@model IEnumerable<Inventario_Productos_Tecnologicos.Models.Categorias>

@foreach (var categoria in Model)
{
    @if (categoria.Subcategorias.Any())
    {
        <li class="dropdown-container">
            <a asp-controller="Productos"
               asp-action="PorCategoria"
               asp-route-id="@categoria.Id"
               class="dropbtn">
                @categoria.Nombre
            </a>
            <div class="dropdown-content">
                @foreach (var subcategoria in categoria.Subcategorias)
                {
                    <a asp-controller="Productos"
                       asp-action="PorSubcategoria"
                       asp-route-id="@subcategoria.Id">
                        @subcategoria.Nombre
                    </a>
                }
            </div>
        </li>
    }
}
