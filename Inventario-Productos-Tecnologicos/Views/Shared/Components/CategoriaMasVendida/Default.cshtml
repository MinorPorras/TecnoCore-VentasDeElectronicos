@model System.Collections.Generic.List<Inventario_Productos_Tecnologicos.Models.ViewModels.CategoriaMasVendidaViewModel>


<div class="categoriasMasVendidas">
    <h4>Categorías más vendidas</h4>
    <div class="stats-table">
        <div class="stats-table-header">
            <h5>Categorías</h5>
            <h5>Total</h5>
        </div>
        
        @if (Model.Count == 0)
        {
            <div class="stats-item">
                <h6>No hay categorías registrados</h6>
            </div>
            if (Model.Count < 5)
            {
                for (var i = Model.Count; i < 4; i++)
                {
                    <div class="stats-item">
                        <h6>---------------------------</h6>
                    </div>
                }
            }
        }
        else
        {
            foreach (var categoria in Model)
            {
                <div class="stats-item">
                    <h6>@categoria.subCategoria.TC_Nombre</h6>
                    <p>@categoria.TotalVentas.ToString()</p>
                </div>
            }
            if (Model.Count < 5)
            {
                for (var i = Model.Count; i < 5; i++)
                {
                    <div class="stats-item">
                        <h6>---------------------------</h6>
                    </div>
                }
            }
        }
    </div>
</div>